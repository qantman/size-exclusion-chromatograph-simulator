function objToStr(obj) {
    if(typeof(obj) == "object"){
        let str = ""
        for (let key in obj){
            str = str + `${obj[key]} `
        }
        return str
    }else{
        return 1
    }
}
module.exports = objToStr
